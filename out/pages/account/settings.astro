---
import { getLocale } from '@/lib/i18n';

return Astro.redirect(
  getLocale(Astro.url.pathname) === 'de'
    ? '/de/dashboard#account-settings'
    : getLocale(Astro.url.pathname) === 'en'
      ? '/en/dashboard#account-settings'
      : '/dashboard#account-settings',
);
---
