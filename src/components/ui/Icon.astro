---
interface Props {
  name:
    | 'celebration'
    | 'rocket'
    | 'chart'
    | 'lightbulb'
    | 'search'
    | 'key'
    | 'refresh'
    | 'tool'
    | 'palette'
    | 'laptop'
    | 'photo'
    | 'noise'
    | 'preset'
    | 'sliders'
    | 'batch'
    | 'box'
    | 'mail'
    | 'gift'
    | 'edit'
    | 'target'
    | 'book'
    | 'plug'
    | 'clipboard'
    | 'check'
    | 'bell'
    | 'chat'
    | 'statusDot'
    | 'mouse'
    | string;
  class?: string;
  ariaLabel?: string;
}
const { name, class: cls = '', ariaLabel = undefined } = Astro.props as Props;
const svgClass = cls || 'w-6 h-6';
const ariaHidden = !ariaLabel;
---

{/* Celebration / sparkles */}
{
  name === 'celebration' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M12 3v2M12 19v2M4.2 7.2l1.4 1.4M18.4 15.4l1.4 1.4M3 12h2M19 12h2M4.2 16.8l1.4-1.4M18.4 8.6l1.4-1.4"
      />
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M12 7a5 5 0 100 10 5 5 0 000-10z"
      />
    </svg>
  )
}

{/* Rocket */}
{
  name === 'rocket' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M9 18c-2 0-4-2-4-2s2-2 2-4 2-4 4-4 4 2 4 4-2 4-4 4z"
      />
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M13 11l6-6M15 3l6 6"
      />
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M7 17s1 2 5 2 5-2 5-2"
      />
    </svg>
  )
}

{/* Chart (bar chart) */}
{
  name === 'chart' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <rect x="3" y="11" width="4" height="8" rx="1" fill="currentColor" />
      <rect x="9" y="7" width="4" height="12" rx="1" fill="currentColor" />
      <rect x="15" y="3" width="4" height="16" rx="1" fill="currentColor" />
    </svg>
  )
}

{/* Lightbulb */}
{
  name === 'lightbulb' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 18h6M10 21h4" />
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M9 13a4 4 0 116 0c0 1.657-1 2-1.5 3H10.5C10 15 9 14.657 9 13z"
      />
    </svg>
  )
}

{/* Search */}
{
  name === 'search' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <circle cx="11" cy="11" r="6" stroke="currentColor" stroke-width="1.5" fill="none" />
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M21 21l-4.35-4.35"
      />
    </svg>
  )
}

{/* Key */}
{
  name === 'key' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 2l-6 6" />
      <circle cx="7" cy="17" r="3" fill="currentColor" />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10 14l11-11" />
    </svg>
  )
}

{/* Refresh */}
{
  name === 'refresh' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M21 12a9 9 0 11-3.22-6.78L21 6"
      />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 6v6h-6" />
    </svg>
  )
}

{/* Tool (wrench) */}
{
  name === 'tool' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M14 3l7 7-4 4-7-7"
      />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 14l-6 6" />
    </svg>
  )
}

{/* Palette */}
{
  name === 'palette' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path d="M12 3C7 3 3 7 3 12s4 6 9 6a3 3 0 003-3 3 3 0 10-3-3" fill="currentColor" />
      <circle cx="7.5" cy="9.5" r="0.8" fill="currentColor" />
      <circle cx="11" cy="7" r="0.8" fill="currentColor" />
      <circle cx="16.5" cy="9" r="0.8" fill="currentColor" />
    </svg>
  )
}

{/* Laptop */}
{
  name === 'laptop' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <rect
        x="3"
        y="5"
        width="18"
        height="12"
        rx="1.5"
        stroke="currentColor"
        stroke-width="1.5"
        fill="none"
      />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M2 19h20" />
    </svg>
  )
}

{/* Photo (existing) */}
{
  name === 'photo' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M3 7l6 5 4-3 4 3 4-5v10a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V7z"
      />
    </svg>
  )
}

{/* Noise (existing) */}
{
  name === 'noise' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M12 2l1.5 3L17 6.5l-3 1.5L12 12l-1.5-3L7 7.5 10 6 12 2z"
      />
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M5 20l2-2M19 20l-2-2"
      />
    </svg>
  )
}

{/* Preset / sliders */}
{
  name === 'preset' ||
    (name === 'sliders' && (
      <svg
        class={svgClass}
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
        aria-hidden={ariaHidden}
        role={ariaLabel ? 'img' : undefined}
        aria-label={ariaLabel}
      >
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 6h16" />
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 12h10" />
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 18h8" />
        <circle cx="7" cy="12" r="1.5" fill="currentColor" />
        <circle cx="12" cy="18" r="1.5" fill="currentColor" />
      </svg>
    ))
}

{/* Batch (existing) */}
{
  name === 'batch' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <rect
        x="3"
        y="6"
        width="14"
        height="12"
        rx="2"
        stroke="currentColor"
        stroke-width="1.5"
        fill="none"
      />
      <rect
        x="7"
        y="4"
        width="14"
        height="12"
        rx="2"
        stroke="currentColor"
        stroke-width="1.5"
        fill="none"
      />
    </svg>
  )
}

{/* Box */}
{
  name === 'box' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M3 7l9-4 9 4v10l-9 4-9-4V7z"
      />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 3v14" />
    </svg>
  )
}

{/* Mail */}
{
  name === 'mail' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <rect
        x="3"
        y="5"
        width="18"
        height="14"
        rx="2"
        stroke="currentColor"
        stroke-width="1.5"
        fill="none"
      />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 7l9 6 9-6" />
    </svg>
  )
}

{/* Gift */}
{
  name === 'gift' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <rect
        x="3"
        y="8"
        width="18"
        height="10"
        rx="2"
        stroke="currentColor"
        stroke-width="1.5"
        fill="none"
      />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8v10" />
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M12 8c-1.5-3-5-4-7-2 0 0 2 1.5 4 2"
      />
    </svg>
  )
}

{/* Edit (pencil) */}
{
  name === 'edit' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M3 21l3-1 11-11 1-3-3 1L4 20z"
      />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M14 7l3 3" />
    </svg>
  )
}

{/* Target (bullseye) */}
{
  name === 'target' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <circle cx="12" cy="12" r="8" stroke="currentColor" stroke-width="1.5" fill="none" />
      <circle cx="12" cy="12" r="4" fill="currentColor" />
      <circle cx="12" cy="12" r="2" stroke="currentColor" stroke-width="1.5" fill="none" />
    </svg>
  )
}

{/* Book */}
{
  name === 'book' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 5h14v14H3z" />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 7v10" />
    </svg>
  )
}

{/* Plug */}
{
  name === 'plug' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M7 7v4a4 4 0 004 4h2v2a1 1 0 001 1h2v-4l-4-4"
      />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M7 3v4" />
    </svg>
  )
}

{/* Clipboard */}
{
  name === 'clipboard' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <rect x="8" y="2" width="8" height="4" rx="1" fill="currentColor" />
      <rect
        x="4"
        y="6"
        width="16"
        height="14"
        rx="2"
        stroke="currentColor"
        stroke-width="1.5"
        fill="none"
      />
    </svg>
  )
}

{/* Check */}
{
  name === 'check' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
    </svg>
  )
}

{/* Bell */}
{
  name === 'bell' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M15 17h5l-1-5a7 7 0 00-14 0l-1 5h5"
      />
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M13.73 21a2 2 0 01-3.46 0"
      />
    </svg>
  )
}

{/* Chat / speech bubble */}
{
  name === 'chat' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z"
      />
    </svg>
  )
}

{/* Status dot (small filled circle) */}
{
  name === 'statusDot' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 8 8"
      fill="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <circle cx="4" cy="4" r="4" />
    </svg>
  )
}

{/* Mouse */}
{
  name === 'mouse' && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <rect
        x="7"
        y="3"
        width="10"
        height="18"
        rx="5"
        stroke="currentColor"
        stroke-width="1.5"
        fill="none"
      />
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 7v4" />
    </svg>
  )
}

{/* Fallback: generic circle */}
{
  ![
    'photo',
    'noise',
    'preset',
    'sliders',
    'batch',
    'tool',
    'celebration',
    'rocket',
    'chart',
    'lightbulb',
    'search',
    'key',
    'refresh',
    'palette',
    'laptop',
    'box',
    'mail',
    'gift',
    'edit',
    'target',
    'book',
    'plug',
    'clipboard',
    'check',
    'bell',
    'chat',
    'statusDot',
    'mouse',
  ].includes(name) && (
    <svg
      class={svgClass}
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
      aria-hidden={ariaHidden}
      role={ariaLabel ? 'img' : undefined}
      aria-label={ariaLabel}
    >
      <circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.5" fill="none" />
    </svg>
  )
}
