---
import AuthLayout from '@/layouts/AuthLayout.astro';
---

<AuthLayout title="Password Reset Sent">
  <div class="text-center">
    <h1 class="text-2xl font-bold mb-4">Link zum Zurücksetzen des Passworts gesendet</h1>
    <p class="text-gray-600 dark:text-gray-400">
      Wenn ein Benutzer mit dieser E-Mail-Adresse existiert, wurde ein Link zum Zurücksetzen des Passworts gesendet. Bitte überprüfen Sie Ihren Posteingang.
    </p>
  </div>
</AuthLayout>

<script>
  (() => {
    try {
      const url = new URL(window.location.href);
      const params = url.searchParams;
      let mutated = false;
      ['success', 'error'].forEach((key) => {
        if (params.has(key)) {
          params.delete(key);
          mutated = true;
        }
      });
      if (mutated) {
        const newQuery = params.toString();
        const newUrl = url.pathname + (newQuery ? `?${newQuery}` : '') + url.hash;
        window.history.replaceState({}, document.title, newUrl);
      }
    } catch (e) {
      console.error('[PasswordResetSent] URL cleanup failed:', e);
    }
  })();
</script>